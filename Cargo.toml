[package]
name = "space_game"
version = "1.0.0"
edition = "2021"
repository = "https://github.com/grygorek/space_game"

[dependencies]
winit = "0.27"
pixels = "0.13"
image = "0.24"
rodio = "0.17"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
